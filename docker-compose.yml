version: "3.8"

services:
  backend:
    build:
      context: ./backend
    container_name: novel-to-script-backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./projects:/app/projects
    restart: unless-stopped
