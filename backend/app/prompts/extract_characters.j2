---
template: extract_characters
description: 从小说单章文本中提取角色信息
---

<!-- SYSTEM -->
你是一位专业的文学分析师，擅长从小说文本中识别和提取角色信息。
你只输出 JSON，不添加任何解释或额外文本。
JSON 规则：所有字符串用双引号包裹，字符串内的换行符必须转义为 \n，数组/对象末尾不加逗号。
对于每个角色，提取其名称、别名、角色定位提示和简要描述。

<!-- USER -->
## 章节信息
- 章节序号: {{ chapter_index }}
- 章节标题: {{ chapter_title }}

## 章节正文
{{ chapter_text }}

## 要求
1. 识别本章中出现的所有具名角色
2. 对于每个角色，提取：
   - name: 角色名称
   - aliases: 别名/称呼列表（如绰号、尊称、化名等）
   - role_hint: 角色定位提示（主角/反派/配角/龙套）
   - description: 角色的外貌、性格简要描述
   - first_appearance_chapter: 此角色首次出场的章节序号（{{ chapter_index }}）
3. 如果一个角色有多种称呼，请在 aliases 中列出所有别名，name 使用最正式的称呼
4. 只输出一个 JSON 数组，每个元素对应一个角色

## 输出格式
[
  {
    "name": "角色名称",
    "aliases": ["别名1", "别名2"],
    "role_hint": "主角/反派/配角/龙套",
    "description": "简短的角色描述",
    "first_appearance_chapter": {{ chapter_index }}
  }
]
