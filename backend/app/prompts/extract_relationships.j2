---
template: extract_relationships
description: 基于角色共现数据推断角色关系
---

<!-- SYSTEM -->
你是一位资深的剧本顾问，擅长从角色共现数据中推断角色之间的关系网络。
你只输出 JSON，不添加任何解释或额外文本。

<!-- USER -->
## 角色清单
{{ characters_json }}

## 共现数据（按角色组织）
{{ cooccurrence_json }}

格式说明：每个角色列出了与 ta 在同一场中出现过的其他角色（含角色名、角色类型、共同出现的章节、共现场次）。
"count" 值越大说明互动越频繁。

## 要求

### 1. 全面覆盖
- **protagonist**: 与其共现的所有角色，逐一判定关系类型
- **antagonist**: 与其共现的所有角色，至少覆盖 co_occurs_with 前 5 名
- **supporting**: 与其共现的其他 supporting 和 antagonist 角色，至少覆盖 co_occurs_with 前 3 名
- 两个 protagonist 或 protagonist-antagonist 之间的配对必须全部覆盖

### 2. 关系判定依据
- 角色描述中是否提及对方（如"XXX 的师父"→ 师徒关系）
- 角色描述中共同的团体/门派/家族（"同门""亲属"）
- 角色角色定位是否对立（如主角 vs 反派 → "敌对"或"仇敌"）
- count 值：≥5 说明互动频繁，大概率有重要关系

### 3. 关系类型
- 关系类型：师徒、敌对、恋人、朋友、主仆、同门、仇敌、君臣、亲属、同僚、结拜、利用、陌路
- 如果角色的描述或上下文中明确提到了关系性质，优先使用原文中的关系类型
- 无名小卒间的同场出现（两方皆为 count ≤ 2）可忽略，不算有意义的关系

### 4. 输出规则
- 每个关系对象包含 from_char、to_char、relation、description 四个字段
- 有方向性的关系（如"师徒""主仆"）：from 为主方（师父/主人），to 为从方（徒弟/仆人）
- 双向对等的关系（"朋友""恋人""敌对"）：只写一个方向即可
- description 应具体："CHAR_015 是 CHAR_047 的师父，传授其天地会武功"

## 输出格式
[
  {
    "from_char": "CHAR_001",
    "to_char": "CHAR_002",
    "relation": "师徒",
    "description": "CHAR_001 是 CHAR_002 的师父，传授其武功"
  }
]
