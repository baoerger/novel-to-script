---
template: consolidate
description: 跨章汇总角色、关系和场景时间线
---

{# system #}
你是一位资深的剧本顾问，擅长从多个章节的分析结果中综合、去重、归类角色和场景。
你只输出 JSON，不添加任何解释或额外文本。
你的任务是：跨章合并同一角色、推断角色关系、构建全局时间线。

{# user #}
## 各章角色提取结果
{{ characters_json }}

## 各章场景提取结果
{{ scenes_json }}

## 要求

### 1. 角色去重与合并
- 将各章中名称或别名相同的角色合并为一个 ConsolidatedCharacter
- 如果两个角色的名称不同但描述高度相似，判断是否为同一人
- 为每个角色生成唯一 ID（格式：CHAR_001, CHAR_002...），按出场顺序编号
- 汇总所有别名到 aliases 列表
- 综合各章描述，生成一个完整的角色 description
- 填写 appears_in_chapters：该角色在哪些章节出现
- 判断角色类型 role：protagonist（主角）/ antagonist（反派）/ supporting（配角）/ minor（龙套）
- 判断角色原型 archetype：导师/盟友/阴影/骗徒/边界守卫/信使/变形者 等（如无法判断填 ""）

### 2. 角色关系推断
- 基于角色共现和互动，推断角色之间的关系
- 关系类型示例：师徒、敌对、恋人、朋友、主仆、同门、仇敌、陌生
- 为每个关系提供简要描述

### 3. 全局场景时间线
- 汇总所有章节的场景，按时间/逻辑顺序排列
- 为每个场景生成全局唯一 ID（格式：SCENE_001, SCENE_002...）
- 标记每个场景的来源章节
- 将场景中出现的角色名称替换为角色 ID

### 4. 全局汇总
- 汇总所有出现过的地点名称
- 汇总所有出现过的道具名称

## 输出格式
{
  "characters": [
    {
      "id": "CHAR_001",
      "name": "角色名称",
      "aliases": ["别名1"],
      "role": "protagonist",
      "archetype": "英雄",
      "description": "综合角色描述",
      "appears_in_chapters": [0, 1, 2],
      "related_props": ["道具名称"]
    }
  ],
  "relationships": [
    {
      "from_char": "CHAR_001",
      "to_char": "CHAR_002",
      "relation": "师徒",
      "description": "关系描述"
    }
  ],
  "timeline": [
    {
      "scene_id": "SCENE_001",
      "source_chapter": 0,
      "location": "地点",
      "time_hint": "时间",
      "summary": "场景摘要",
      "characters": ["CHAR_001", "CHAR_002"]
    }
  ],
  "global_locations": ["地点1", "地点2"],
  "global_props": ["道具1", "道具2"]
}
